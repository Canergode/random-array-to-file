# Random Array to File

This project generates N random numbers into a dynamically allocated array, writes them to a text file, then reads and prints the file contents.

## What I Learned
- How to combine dynamic memory allocation with file operations
- How to generate random numbers using rand() and srand()
- How to write and read a file in the same program
- How to use pointer arithmetic with dynamically allocated arrays
